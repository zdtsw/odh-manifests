apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: jupyterhub
  name: traefik-proxy
spec:
  host: ''
  port:
    targetPort: admin
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: traefik-proxy
    weight: 100
